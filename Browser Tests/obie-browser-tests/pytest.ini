[pytest]
addopts = -v --tb=short
testpaths = tests
# Playwright pytest plugin options can be passed on the CLI, e.g.:
#   pytest --headed            run with a visible browser
#   pytest --browser chromium  (chromium is required: app needs Chrome/Edge APIs)
#   pytest --slowmo 300        slow actions down to watch them
